{
  "name": "psfcp-tests",
  "version": "1.0.0",
  "description": "Jest + ws + ajv test harness for PSFCP (port 3030)",
  "scripts": {
    "test": "jest --runInBand --detectOpenHandles"
  },
  "dependencies": {
    "ajv": "^6.12.6",
    "ws": "^8.13.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  }
}

